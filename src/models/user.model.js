class User {
    constructor(dob, firstName, lastName, username, password, role){
        this.dob = dob,
        this.firstName = firstName,
        this.lastName = lastName,
        this.username = username,
        this.password = password,
        this.role = role
    }
}

export default User;