class User {
    constructor(user_id, dob, firstName, lastName, username, password, role){
        this.user_id = user_id,
        this.dob = dob,
        this.firstName = firstName,
        this.lastName = lastName,
        this.username = username,
        this.password = password,
        this.role = role
    }
}

export default User;